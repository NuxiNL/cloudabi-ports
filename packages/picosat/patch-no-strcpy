--- picosat.c
+++ picosat.c
@@ -1140,10 +1140,13 @@
 static void
 new_prefix (PS * ps, const char * str)
 {
+  size_t len;
+
   delete_prefix (ps);
   assert (str);
-  ps->prefix = new (ps, strlen (str) + 1);
-  strcpy (ps->prefix, str);
+  len = strlen (str) + 1;
+  ps->prefix = new (ps, len);
+  memcpy (ps->prefix, str, len);
 }
 
 static PS *

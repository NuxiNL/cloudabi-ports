--- Modules/posixmodule.c
+++ Modules/posixmodule.c
@@ -3662,7 +3662,9 @@
         }
 
         Py_BEGIN_ALLOW_THREADS
+#ifdef HAVE_OPENDIR
         dirp = opendir(name);
+#endif
         Py_END_ALLOW_THREADS
     }
 
@@ -12274,7 +12276,9 @@
 
     errno = 0;
     Py_BEGIN_ALLOW_THREADS
+#ifdef HAVE_OPENDIR
     iterator->dirp = opendir(path);
+#endif
     Py_END_ALLOW_THREADS
 
     if (!iterator->dirp) {

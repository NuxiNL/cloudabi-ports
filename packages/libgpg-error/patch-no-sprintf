--- src/estream-printf.c
+++ src/estream-printf.c
@@ -1119,10 +1119,10 @@
   *--p = '%';
 #ifdef HAVE_LONG_DOUBLE
   if (use_dbl)
-    sprintf (numbuf, p, adblfloat);
+    snprintf (numbuf, sizeof(numbuf), p, adblfloat);
   else
 #endif /*HAVE_LONG_DOUBLE*/
-    sprintf (numbuf, p, afloat);
+    snprintf (numbuf, sizeof(numbuf), p, afloat);
   p = numbuf;
   n = strlen (numbuf);
   pend = p + n;

--- util/getarg.c
+++ util/getarg.c
@@ -215,7 +215,7 @@
      */
     if (LocalToken == NULL) {
         LocalToken = (char *)malloc(3);
-        strcpy(LocalToken, "-?");
+        memcpy(LocalToken, "-?", 3);
     }
 
     /* Check if last item is an option. If not then copy rest of command

--- binutils/ar.c
+++ binutils/ar.c
@@ -671,6 +671,10 @@
 
 int main (int, char **);
 
+int sort_name(const void *a, const void *b) {
+  return strcmp(*(char **)a, *(char **)b);
+}
+
 int
 main (int argc, char **argv)
 {
@@ -793,6 +797,7 @@
 	continue;
 
       files = (file_count > 0) ? argv + arg_index : NULL;
+      qsort(files, file_count, sizeof(*files), sort_name);
 
       arch = open_inarch (inarch_filename,
 			  files == NULL ? (char *) NULL : files[0]);

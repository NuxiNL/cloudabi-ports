--- makefile.include
+++ makefile.include
@@ -14,12 +14,6 @@
   PREFIX=
 endif
 
-ifeq ($(CC),cc)
-  CC = $(PREFIX)gcc
-endif
-LD=$(PREFIX)ld
-AR=$(PREFIX)ar
-RANLIB=$(PREFIX)ranlib
 
 ifndef MAKE
    MAKE=make
